{
  "name": "nuclo-example-styling",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A live showcase of every feature in nuclo's atomic CSS-in-TS styling system.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "nuclo": "workspace:*",
    "typescript": "~6.0.3",
    "vite": "^8.0.14"
  }
}
