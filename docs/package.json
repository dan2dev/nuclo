{
  "name": "nuclo-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "HOST=0.0.0.0 BASE_URL=/ bun --tsconfig-override tsconfig.bun.json src/server.ts",
    "clean": "rm -f .*.bun-build",
    "build": "bun run clean && bun run update:stats && tsc && vite build && bun build-server.ts",
    "update:stats": "bun scripts/update-doc-stats.ts",
    "start": "HOST=0.0.0.0 bun start.ts",
    "preview": "bun run build && bun run start",
    "stress:front": "bun --cwd ../packages/nuclo-core run build && bun run build && bunx playwright test tests/front-stress.spec.ts",
    "stress:front:strict": "bun --cwd ../packages/nuclo-core run build && bun run build && STRESS_STRICT=true bunx playwright test tests/front-stress.spec.ts",
    "stress:front:ui": "bun --cwd ../packages/nuclo-core run build && bun run build && bunx playwright test tests/front-stress.spec.ts --headed --project=stress-chromium",
    "stress:server": "bun tests/server-stress.ts",
    "preview:macos": "bun run build && ./build/server-bun-darwin-arm64",
    "deploy:cloudflare": "bun --cwd ../packages/nuclo-core run build && bun run clean && bun run update:stats && tsc && vite build && wrangler pages deploy build/dist --commit-dirty=true",
    "cf:dev": "bun run build && wrangler pages dev build/dist"
  },
  "dependencies": {
    "nuclo": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.2.0",
    "typescript": "~7.0.2",
    "vite": "^8.2.2",
    "wrangler": "^4.125.0"
  }
}
